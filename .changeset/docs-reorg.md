---
"querybridge-mcp": patch
---

Docs reorg, no functional change. Republishing so the npm package page shows the updated README (table of contents, HTTP transport promoted to a top-level section, consistent tool tables across all 9 families, slim Project structure listing). No code changes — `dist/` output is byte-identical to v0.11.0.
